#include <fstream>
#include <iostream>
#include <string>
using namespace std;

int main(){
	int edad;
	int extranjero = 500;
	int sueldo = 2500;
	int total = 0;	
	
	total = extranjero + sueldo;
	
	cout<<"ingrese edad del jugador: "<<endl;
	cin>>edad;
	cout<<"ingrese si el judador es extranjero o residente: "<<endl;
	cin>>extranjero>>sueldo;
	
	if (edad > 15){
	
		int edad = sueldo + 1400;
			else (edad > 21)
	 		int edad = sueldo + 1200;
	 		else (edad > 30)
	 		int edad = sueldo + 800;
	} 	
	cout<<"el sueldo a pagar es de: "<<sueldo;
	
	
		return 0;
}
